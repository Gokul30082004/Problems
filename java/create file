import java.io.*;
public class file{
    public static void main(String[] args) throws Exception{
        File myfile=new File("filetxt.txt");
        PrintWriter oup=new PrintWriter(myfile);
        /*
        FileOutPutStream f=new FileOutputStream("f.txt");
        PrintWriter oup=new PrintWriter(myfile);  //add text without deleting existing text
        oup.println("asdf);
        */
        oup.println("Firs!");
        oup.close();
    }
}
